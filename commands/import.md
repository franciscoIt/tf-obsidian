# import

Imports existing infrastructure into Terraform state without recreating it.

```terraform
terraform import aws_instance.web i-1234567890abcdef0       # import a resource by its provider ID
```
