# Terraform Overview

## Core Components

![[terraform-core-components.png]]


 The `.terraform.lock.hcl` file

It's a file Terraform automatically creates and updates to record the **exact versions** of providers your project is using. Think of it as a "snapshot" of your dependencies at a point in time.